---
import "../styles/global.css"
import { ClientRouter } from "astro:transitions"
import background from "../assets/background.svg"
import Sidebar from "../components/Sidebar.astro"

interface Props {
  title: string
}

const { title } = Astro.props
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="shortcut icon" href="favicon.ico" type="image/x-icon" />
    <title>UTP+trace - {title}</title>
    <ClientRouter />
  </head>
  <body class="bg-gray-50">
    <img
      id="background"
      src={background.src}
      alt=""
      fetchpriority="high"
      class="w-full h-full fixed top-0 left-0 blur-[100px] -z-10 pointer-events-none"
    />
    <Sidebar />
    <main
      class="lg:ml-72 pt-4 lg:pt-6 px-4 lg:px-6 min-h-screen transition-all duration-300 ease-in-out mt-16 lg:mt-0"
    >
      <slot />
    </main>
  </body>
</html>
